package gasi.gps.core.api.domain.model;

/**
 * Client platform used for platform-specific behavior or auditing.
 *
 * @since 1.0.0
 */
public enum Platform {
    /** Browser or desktop web client. */
    WEB,
    /** Mobile application client. */
    MOBILE
}
